import React, { Component } from 'react';

class FutureAnimation extends Component {
  constructor(props) {
    super(props);
    this.state = {
      // initial state
    };
  }

  // lifecycle methods, event handlers, etc.

  render() {
    return (
      <div>
        Future Animation Placeholder - {this.props.animationSecond}
      </div>
    );
  }
}

export default FutureAnimation;